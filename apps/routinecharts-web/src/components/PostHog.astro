---
// PostHog + Vercel Analytics — included once via BaseLayout.
// Init lives in src/lib/analytics/posthog-client.ts. Tags every event with
// `app: "routinecharts"` for cross-site funnels in CC's shared PostHog
// project (110135). cc-link-tracking auto-fires cc_cta_click on any
// outbound chunkycrayon.com click (the funnel conversion event).
---

<script>
  import "../lib/analytics/posthog-client";
  import "../lib/analytics/cc-link-tracking";
  import "../lib/analytics/blog-view-tracking";
</script>

/**
 * Fun-name generator — re-exported from the shared package so web + mobile
 * suggest names from the same pool. Implementation lives in
 * `@one-colored-pixel/coloring-core/characters` (pure, no Node deps).
 */

export { generateCharacterName } from '@one-colored-pixel/coloring-core/characters';

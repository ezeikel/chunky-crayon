# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist

# TypeScript incremental build cache (per-app, regenerated every build)
*.tsbuildinfo


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# Mobile builds
*.apk
*.ipa
*.aab

# iOS build artifacts
ios/build/
ios/Pods/
*.xcworkspace
*.xcuserdata/

# Android build artifacts
android/build/
android/app/build/
android/.gradle/
packages/db/src/generated/
.playwright-mcp/
apps/chunky-crayon-web/.env.local.bak-*

# IDE / editor state
.claude/
.cursor/

# Worker temp output (mp4s, mp3s, intermediate render artifacts)
apps/chunky-crayon-worker/tmp/

# Storybook build artifacts + logs
packages/coloring-ui/storybook-static/
packages/coloring-ui/debug-storybook.log

# Translations review reports — generated, not source
packages/translations/reports/

# Debug screenshots at repo root (ad-*.png, footer-*.png, pricing-*.png, etc.)
/*.png
/*.jpg
